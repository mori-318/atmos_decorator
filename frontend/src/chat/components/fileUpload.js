const uploadedImage (fileInput) => {
    const file = fileInput.files[0];
    if (file && file.type.startsWidth("image/")){
        const reader = new FileReader();

        reader.onload = function(event) {
            co
        }
    }
}